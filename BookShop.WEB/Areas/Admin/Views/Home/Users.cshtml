@model BookShop.WEB.Models.UserViewModel

<div class="page-heading contact-heading header-text">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="text-content">
                    <h4>Таблица</h4>
                    <h2>Пользователи</h2>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="products">
    <div class="container">
        <div class="AddNewItem">
            <a href="">Добавить нового пользователя</a>
        </div>
        <div class="serchDiv">
            <form>
                <input name="serch" class="serchInput" type="text" placeholder="Поиск" />
            </form>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <td><b>ID:</b></td>
                    <td><b>Логин:</b></td>
                    <td><b>Email:</b></td>

                    <td><b>Управление</b></td>
                </tr>
            </thead>
            <tbody>
                @if (Model.user.Count() > 0)
                {
                    @foreach (var obj in Model.user)
                    {
                        <form asp-area="Admin"  method="post">
                        <tr>
                            <td>@obj.Id</td>
                            <td>@obj.UserName</td>
                            <td>@obj.Email</td>

                            <td>
                                <a>Изменить</a>
                                <button type="submit" value="Удалить">Удалить</button>

                            </td>
                        </tr>
                        </form>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
